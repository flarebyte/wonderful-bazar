package com.flarebyte.cm.com.core.owls.control;

/**
 * The If-Then-Else class is a control construct that has properties
 * ifCondition, then and else holding different aspects of the If-Then-Else. Its
 * semantics is intended as ``Test If-condition; if True do Then, if False do
 * Else.'' (Note that the class Condition, which is a placeholder for further
 * work, will be defined as a class of logical expressions.)
 * 
 * @author olivier
 * 
 */
public interface IfThenElse {

}
