package com.flarebyte.cm.com.core.owls.control;

/**
 * Here the process consists of concurrent execution of a bunch of process
 * components with barrier synchronization. That is, Split+Join completes when
 * all of its components processes have completed. With Split and Split+Join, we
 * can define processes that have partial synchronization (e.g., split all and
 * join some sub-bag).
 * 
 * @author olivier
 * 
 */
public interface SplitJoin {

}
